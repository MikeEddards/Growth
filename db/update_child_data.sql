update age_data set age = ${age}
where data_id = ${data_id};

update age_data set height = ${height}
where data_id = ${data_id};

update age_data set weight = ${weight}
where data_id = ${data_id};

update age_data set head_size = ${head_size}
where data_id = ${data_id};

update age_data set image = ${image}
where data_id = ${data_id};
